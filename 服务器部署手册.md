# 合成大吕布·掉落版 —— 服务器部署手册

> 目标：把游戏（含注册 ID / 云存档 / 排行榜）部署到腾讯云轻量应用服务器上，
> 朋友通过 `http://服务器IP:8080` 直接游玩。全程免备案（不绑域名）。

---

## 0. 文件说明

```
E:\work\game\
├── 合成大吕布-掉落版.html   ← 游戏本体（前端 + 内嵌物理引擎 + 头像）
└── server\
    └── server.js            ← 后端（零依赖，Node ≥ 18，数据存 users.json）
```

部署只需要这两个文件。后端启动后自动托管游戏页面，**一个端口搞定前后端**。

> ## ⚡ 一键部署（推荐）
>
> 手册第 2、3、4 步可以合并成一条命令。上传三个文件到服务器后执行：
>
> ```bash
> bash deploy.sh
> ```
>
> 脚本会自动完成：检查/安装 Node.js → 写入 systemd 服务 → 启动 → 自检 → 打印访问地址。
> 支持重复执行（更新代码后再跑一遍 = 重启服务）。自定义端口：`PORT=9000 bash deploy.sh`。
>
> 上传（在本机 PowerShell，`<IP>` 换成你的公网 IP；**只传这三个文件**，
> 不要传整个 game 目录——里面的 test\node_modules 有 25MB 开发依赖，服务器用不上）：
>
> ```powershell
> ssh ubuntu@<IP> "sudo mkdir -p /opt/lvbu && sudo chown ubuntu /opt/lvbu"
> scp "E:\work\game\server\server.js" ubuntu@<IP>:/opt/lvbu/server/
> scp "E:\work\game\合成大吕布-掉落版.html" ubuntu@<IP>:/opt/lvbu/
> scp "E:\work\game\deploy.sh" ubuntu@<IP>:/opt/lvbu/
> ssh ubuntu@<IP> "cd /opt/lvbu && bash deploy.sh"
> ```
>
> 跑完只剩一件事：**腾讯云控制台防火墙放行 TCP 8080**（脚本做不了这步）。
> 下面的 2-4 节是手动分步版，想理解每一步在做什么可以照着看。

---

## 1. 购买轻量应用服务器

1. 打开 [腾讯云轻量应用服务器](https://cloud.tencent.com/product/lighthouse) → 立即选购
2. 配置建议：
   - **镜像**：`Ubuntu 24.04 LTS`（纯系统，不要选应用镜像）
   - **地域**：离玩家近的（广东/上海等）
   - **规格**：最低配 2核2G 足够（这游戏后端负载约等于零）
   - **时长**：先买 1 个月试水，玩得开心再续
3. 买完进控制台，记下 **公网 IP**
4. **防火墙放行端口**（关键！）：控制台实例详情 → 防火墙 → 添加规则：
   - 协议 `TCP`，端口 `8080`，策略 `允许`，备注 `游戏`

---

## 2. 服务器上装 Node.js（一次性）

用腾讯云网页终端（控制台实例卡片上的「登录」按钮，免配 SSH）或本机 SSH 登录后执行：

```bash
# 安装 Node.js 22 LTS
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt-get install -y nodejs
node -v   # 应显示 v22.x
```

---

## 3. 上传文件

在本机 PowerShell（游戏目录下）执行，`<IP>` 换成你的公网 IP：

```powershell
# 先在服务器上建目录（也可在网页终端执行）
ssh ubuntu@<IP> "sudo mkdir -p /opt/lvbu && sudo chown ubuntu /opt/lvbu"

# 传文件
scp "E:\work\game\server\server.js" ubuntu@<IP>:/opt/lvbu/
scp "E:\work\game\合成大吕布-掉落版.html" ubuntu@<IP>:/opt/lvbu/
```

> 没配 SSH 密钥的话，第一次会要求输密码（购买时设置的）。
> 或者：网页终端里直接 `nano /opt/lvbu/server.js` 粘贴保存，`nano` 同理传 HTML —— 但 scp 更省事。

---

## 4. 启动并设为常驻（systemd）

在服务器上执行：

```bash
sudo tee /etc/systemd/system/lvbu.service > /dev/null <<'EOF'
[Unit]
Description=lvbu-drop game server
After=network.target

[Service]
WorkingDirectory=/opt/lvbu
ExecStart=/usr/bin/node /opt/lvbu/server.js
Restart=always
RestartSec=3
Environment=PORT=8080

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now lvbu
sudo systemctl status lvbu   # 看到 active (running) 即成功
```

---

## 5. 验证

- 本机浏览器打开 `http://<IP>:8080` → 应看到游戏
- 手机（任意网络）打开同一地址 → 应能玩、能注册 ID、能上排行榜

常见问题：

| 现象 | 原因 |
|---|---|
| 打不开 | 防火墙没放行 8080（回第 1 步第 4 点）|
| 打不开 | `systemctl status lvbu` 看报错；`sudo journalctl -u lvbu -n 50` 看日志 |
| 页面报"游戏页面文件缺失" | HTML 没传到位，确认 `/opt/lvbu/合成大吕布-掉落版.html` 存在 |

---

## 6. 日常更新（改了游戏之后）

本机改完 `合成大吕布-掉落版.html` 后：

```powershell
scp "E:\work\game\合成大吕布-掉落版.html" ubuntu@<IP>:/opt/lvbu/
```

刷新浏览器即生效（无需重启服务；只改了 server.js 才需要 `sudo systemctl restart lvbu`）。

---

## 7. 数据备份

所有用户数据在 `/opt/lvbu/users.json`。定期备份：

```bash
cp /opt/lvbu/users.json /opt/lvbu/backup/users-$(date +%F).json
```

恢复 = 把备份文件改名回 `users.json` 后 `sudo systemctl restart lvbu`。

---

## 8. 安全须知

- **HTTP 明文**：目前是 `http://IP:8080`，密码传输未加密。这是游戏口令，**别用你其他网站的重要密码**。想上 HTTPS 需要域名 + 备案（或 Caddy + 免备案的海外解析，但国内访问会变慢）
- 登录后第一件事：控制台重置服务器密码 / 改用 SSH 密钥登录
- 后端自带：scrypt 密码哈希、请求限流（120 次/分/IP）、参数校验、令牌 30 天过期 —— 朋友间玩足够；真对外开放后建议再看一眼日志
- `users.json` 包含用户昵称和密码哈希（不含明文密码），别把它发到公开场合
